# name = input("What is your name dear?")
# print("Hello + name")

# print(2/3) detta är float, kan ge svar som innehåller decimaltal
# print(2//3) detta är int, kan ge svar som är heltal

# pi = 3.14
# print("the math symbol pi is " + (str(pi)))

# print(a)
# a=45 this code does not work because a = 45 should be before print

# pi = float(3.14) koden är inte helt fel men det blir fel när det står float innan då 3.14 redan är ett float.

# radius = float(input("Radius:"))
# x = 3.14
# pi = x # här är felet. det är bara att skriva pi = 3.14 istället för att skriva med x först
# area = pi  * radius ** 2
# print(area)

# x = 4
# y = 5
# a = ((x) * (y)) #det behövs inga paranteser vid x och y utan det räcker med (x * y)
# print(a)

# x = 4
# y = 5
# a = 3(x + y) # felet är här. det måste stå ett * innan annars fattar ej datorn
# print(a)

# radius = input(float("Enter the radius:")) # det ska vara float först annars blir datorn förvirrad.

# print(2/3+4) # this print 2/3 + 4 and same with the codes below.
# print(2 / 3 + 4)
# print(   2 /    3+    4  )

# A constant is a variable that isn't a variable.
# Variable constants are variables that aren't allowed in python (where values change)

# A single quote or a double-string quote is the same. There is really no difference between them. But if you want to have 
# a quote in side a quote you can use single quotes and double quotes. For an example 'I love this book called "Agent W"'.

# print("This\r\nis\r\nmy\r\nsample") \r\n och \n fungerar lika

# A python program can use single-quotes and/or double-quotes.

# print(3 + 4 + 5 / 3) # you need to have () between 3 and 5 or else it will print 3+4+ 5/3

# An operator is a symbol like + or -

# x = 3 # This code will print out 3 because it skips x+1 because you did not identify it like x = x+1
# x + 1
# print(x)

# user_name = input("Enter your name: )" # wrong it should be user_name = input("Enter your name: ")

# value = int(input(print("Enter your age"))) # wrong. ou don't need print if you have input. it should be
# value = int(input("Enter your age"))